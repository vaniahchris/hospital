'use client';

import { AdminIcon, AdminShell, responses } from '../shared';

export default function Responses(){function exportCsv(){const header='ID,Date,Care,Staff,Wait,Recommend,Score,Comment';const rows=responses.map(r=>[r.id,r.date,r.care,r.staff,r.wait,r.recommend,r.score,`"${r.comment.replaceAll('"','""')}"`].join(','));const blob=new Blob([[header,...rows].join('\n')],{type:'text/csv'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='feedback-responses.csv';a.click();URL.revokeObjectURL(url)}return <AdminShell title="Responses"><main className="admin-content"><div className="admin-heading"><div><h2>Patient responses</h2><p>Review feedback and find areas where the care experience can improve.</p></div><div className="admin-toolbar"><button className="admin-button secondary" onClick={exportCsv}><AdminIcon name="export"/> Export CSV</button><button className="admin-range">Last 30 days ▾</button></div></div><section className="admin-panel"><div className="panel-header"><h3>Recent responses</h3><span>Showing 5 of 1,248</span></div><div className="table-wrap"><table className="response-table"><thead><tr><th>Response</th><th>Date</th><th>Care</th><th>Staff</th><th>Wait time</th><th>Recommend</th><th>Score</th><th>Comment</th></tr></thead><tbody>{responses.map(row=><tr key={row.id}><td><strong>{row.id}</strong></td><td>{row.date}</td><td>{row.care}</td><td>{row.staff}</td><td>{row.wait}</td><td>{row.recommend}</td><td><span className={`score-pill ${row.score>=4?'score-high':'score-mid'}`}>{row.score}</span></td><td className="comment-cell" title={row.comment}>{row.comment}</td></tr>)}</tbody></table></div></section></main></AdminShell>}
